// Define types for API responses
export interface JoinResponse {
  token: string
  // Add any other fields your API returns
}

export interface CheckTokenResponse {
  valid: boolean
  // Add any other fields your API returns
}

// You can add more types as needed for your API
